This repo contains hyqneuron's configuration files... it would be way more
convenient to store this using FTP... 

.vimrc:             ~/.vimrc
openbox-config:     ~/.config/openbox
backgrounds:        ~/.config/backgrounds
